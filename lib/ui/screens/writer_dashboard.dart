import 'package:flutter/material.dart';

class WriterDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Writer Dashboard")),
      body: Center(child: Text("Welcome Writer")),
    );
  }
}
