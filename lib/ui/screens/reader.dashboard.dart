import 'package:flutter/material.dart';

class ReaderDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Reader Dashboard")),
      body: Center(child: Text("Welcome Reader")),
    );
  }
}
