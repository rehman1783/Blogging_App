import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF3A86FF);       // Light blue
  static const secondary = Color(0xFF8338EC);     // Vibrant purple
  static const background = Color(0xFFF9FAFB);    // Light gray background
  static const card = Color(0xFFFFFFFF);          // Card color (white)
  static const textPrimary = Color(0xFF1E1E1E);   // Dark text
  static const textSecondary = Color(0xFF6C757D); // Muted text
  static const border = Color(0xFFE0E0E0);        // Input borders
  static const white = Colors.white;
  static const black = Colors.black;
}
